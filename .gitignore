node_modules/
dist/
.env
.env.*
playwright-report/
test-results/

# Python
__pycache__/
*.pyc

# Local recipe-import working files (not part of the app). Anchored to the repo
# root so it does NOT also ignore spike/import (tracked ops tooling).
/import/
/recipebox/

# Throwaway Playwright config pointing at the pre-installed Chromium (env-specific).
playwright.local.ts

# Build-time snapshot staging (RUN-BUNDLE-PRECACHE). Produced by
# scripts/snapshot.mjs in CI, consumed + stamped by build.mjs. Not committed.
.snapshot-staging/
