node_modules/
dist/
.env
.env.*
playwright-report/
test-results/

# Python
__pycache__/
*.pyc

# Local recipe-import working files (not part of the app). Anchored to the repo
# root so it does NOT also ignore spike/import (tracked ops tooling).
/import/
/recipebox/

# Throwaway Playwright config pointing at the pre-installed Chromium (env-specific).
playwright.local.ts

# Android TWA build (plan 2026-07-18-1): the generated project is disposable
# scratch, and the release keystore must NEVER be committed (D5).
/.android-build/
android.keystore
