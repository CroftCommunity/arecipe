node_modules/
# Local corpus state — never committed. Regenerated by `wbsync`.
state/
raw/
runs/
# Cached Commons renditions + the blob-CID manifest (~190MB). Rebuilt by
# `wbsync images`; the manifest re-resolves and re-uploads on demand.
images/
# Throwaway
*.local.*
