{
  "lexicon": 1,
  "id": "app.arecipe.cookFollow",
  "defs": {
    "main": {
      "type": "record",
      "description": "A public follow of a cook, curating whose recipes fill your arecipe feed and cookbook. arecipe's analog of app.bsky.graph.follow, scoped to the recipe app: one record per followed cook.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["subject", "createdAt"],
        "properties": {
          "subject": {
            "type": "string",
            "format": "did",
            "description": "The DID of the followed cook."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
